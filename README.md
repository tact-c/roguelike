# roguelike
to build on linux:
g++ game.cpp -o build/game
x86_64-w64-mingw32-g++ main.cpp -o program.exe -static

it is still very primitive and all it can do is move an @ in a field of #. idk how much more i will add the game.
